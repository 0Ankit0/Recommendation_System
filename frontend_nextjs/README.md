# Next.js Frontend

This frontend replaces the ASP.NET/Blazor web project with Next.js.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` to the FastAPI URL (default `http://localhost:8000`).
